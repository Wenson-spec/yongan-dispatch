# 找车台回单跟踪现场核验记录

- 访问页面：`/station/find-vehicle`
- 当前回单跟踪页签可见记录为 2 条，页面优先按“正式外请分组”展示。
- 当前可见分组为 `P0000052177`，显示“同批 2 单”“回单跟踪口径”。
- 该分组操作列未出现“标记已寄出”按钮，而是直接显示文案：`当前组头下暂无可操作的回单寄出动作`。
- 因此，在当前预览数据下，无法直接复现“点击后无反应”，更像是前端把可操作资格和分组展示逻辑做错了，导致部分订单被展示为可跟踪但实际不可操作，或把真正可操作的子单隐藏在组内。
- 结合服务端 `order.markPodSent` 已要求 `podOwnership === current_order`，而前端当前筛选 `canMarkSent` 仅依据 `podStatus`，存在前后端资格口径不一致的高度嫌疑。
