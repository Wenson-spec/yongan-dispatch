# 找车台待审批核查记录

- 当前已进入 `/station/find-vehicle` 页面，而不是管理驾驶舱。
- 页面顶部页签显示：`待审批 2`，说明当前环境中确实存在两条待审批数据可供核查。
- 当前可见内容仍是默认的“待找车”表格区域，表格正文为空，尚未切换到“待审批”页签。
- 用户反馈的核心问题仍是：待审批组合审批没有按“主单承载层 + 展开后显示子单明细”的方式展示。
- 下一步需要直接切换到“待审批”页签，核对是否仍是子单平铺、是否缺少主单汇总行，以及审批记录入口是否挂在主单层。

当前实页核查显示，待审批页签已经出现“主单”承载层、整组操作按钮与子单展开区域，但仍存在关键偏差：主单行显示的单号与第一条子单相同，展开后又再次出现同号子单，说明当前主单承载层仍在回退使用首条审批记录，而不是真实的外请主单。页面现象证明问题已从“没有主单层”收敛为“主单识别失败导致首个子单重复渲染”，下一步需要继续修正分组到主单的映射逻辑，使主单行取真实承载订单，子单列表则排除该主单本身。
- 最新展开核查：待审批组合单展开后，子单列表已只剩 1 条，说明“主单不再重复出现在子单区”的去重修复已经生效。
- 当前可见结构为：组头出现“主单”徽标与整组操作，展开区出现 1 条“子单 F0002259687”。这表明“主单承载层 + 展开子单明细”的结构已经落地。
- 仍待确认的问题：组头标题显示 `F0002259693`，而组级复选提示使用的是分组键 `P0000051779`。下一步需要核对 `F0002259693` 是否就是真实主单；若不是，则继续修正组头标题字段映射。
- 2026-04-07 最新核查：找车台“待审批”页签当前已显示“组合主单 P0000051779”，组头已承载审批焦点、路线及整组说明，说明主单承载层已基本落位；下一步继续核对展开后子单是否仅保留从属明细，以及报价、沟通记录、审批动作是否全部统一挂在主单层。
