## 2026-04-06 录单台统一规则复验（阶段记录）

- 已进入 `/station/entry` 的“全部订单”页签。
- 当前页面可见“刷新总表”“按计划分组”“展开全部”等控件，说明总表分组模式已接入真实页面。
- 首屏可见组合组头行，例如 `P0000051779`、`P0000052073`，组头文案显示“主单入口统一处理，子单自动跟随”。
- 组头提供统一的快捷入口（如“前往录单台”“前往指挥台”），尚未展开查看子单明细行内是否已完全禁用单独操作入口。
- 下一步需继续点开计划组，核对子单明细是否仅显示跟随提示、不再出现单独操作按钮；并切换“待分流”“退回待处理”页签继续复验同一规则。

补充复验结果：我在“全部订单”页签展开计划组后，控制台已确认页面当时出现了一条不正确的重复明细——组头对应的主单又被当作子行渲染了一次，因此看起来像“子单仍有前往录单台入口”。随后我已把展开逻辑修正为**展开时仅渲染除组头主单之外的剩余跟随子单**。本次修正后，后续复验应重点确认两点：其一，组头只保留一行主单入口；其二，展开后的真正子单行只显示“子单仅作明细展示/子单跟随主单”提示，不再带独立快捷入口。
