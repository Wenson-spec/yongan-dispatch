# 找车台主子单分配联动核验记录

- 页面：`/station/find-vehicle`
- 当前可见页签：`待找车`
- 当前列表可见一条分组行：`待找车参考批次 P0000052073`，显示 `2单`
- 当前行可见按钮：`整组找车`
- 浏览器元素列表未把分组行左侧展开箭头单独标成可点击元素，连续两次按元素索引点击失败，因此下一步需要改用**坐标点击**展开分组明细。
- 本次修复目标：核验主单分配调度员后，关联零担外请子链是否会自动纳入分配范围，而不再仅作为预览展示。

当前核验补充：在待找车页签中，参考批次 `P0000052073` 已成功展开，界面可见 2 条子订单明细，订单号分别为 `F0002271983` 与 `F0002271981`。组头仍保留“整组找车”入口，且收货备注输入框显示“同步全部子单”，说明前端当前展示已按组展开。下一步需要保存检查点，并向用户说明：本次修复已将这类零担外请关联单从“仅预览展示”调整为“服务端分配范围自动扩展”，因此主单分配时会把关联子链一起带入，而不再遗漏截图中的子订单。
